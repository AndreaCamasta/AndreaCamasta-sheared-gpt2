# AndreaCamasta-sheared-gpt2
This project reproduces the main ideas of the Sheared LLaMA paper on GPT-2. The pipeline performs structured pruning followed by dynamic domain-aware training (Dynamic Batch Loading) to obtain a smaller yet effective language model.
